<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
    <img 
      src="/images/logo.jpg" 
      alt="Logo" 
      width="60" 
      height="60" 
      class="rounded-circle me-2"
      style='border-radius: 50%;
      border: 2px solid #fff;
      box-shadow: 0 1px 6px #5d4a40;'
    >
    <span class="fs-3 fw-bold" 
      style="background: linear-gradient(90deg, #472518, #b98c78); 
              background-clip: text;
             -webkit-background-clip: text;
             color: transparent;">
  Lontraloop
</span>
  </a>

   <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>



    <div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <li class="nav-item"><a class="nav-link" href="/">Início</a></li>
  </ul>
  <div class="d-flex align-items-center">
    <% if (session && session.idUsuario) { %>
      <!-- aqui poderia ter link pro perfil real -->
      <a href="/usuarios/perfil" class="btn btn-outline-secondary btn-sm">Perfil</a>
    <% } else { %>
      <a href="/usuarios/login" class="btn btn-primary btn-sm">Entrar</a>
    <% } %>
  </div>
</div>
</div>
</nav>